// This structure feels a little redundant because there is only one reducer;
// however, the app is ready to scale to however many ducks/reducers we want to
// add.
import jobsReducer from './jobs';

export default jobsReducer;
